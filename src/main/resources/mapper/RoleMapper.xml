<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.exam.mapper.RoleMapper">

    <resultMap id="SysRoleMap" type="com.example.exam.entity.SysRole">
        <id column="id" property="id"/>
        <result column="role_code" property="roleCode"/>
        <result column="role_name" property="roleName"/>
        <result column="description" property="description"/>
    </resultMap>

    <select id="selectByCode" resultMap="SysRoleMap">
        SELECT id, role_code, role_name, description
        FROM exam_system.sys_role
        WHERE role_code = #{roleCode}
    </select>

    <select id="selectAll" resultType="com.example.exam.vo.RoleVO">
        SELECT id, role_code AS roleCode, role_name AS roleName, description
        FROM exam_system.sys_role
        ORDER BY id ASC
    </select>
</mapper>
